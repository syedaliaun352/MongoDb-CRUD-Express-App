## Express + MongoDB API
A simple RESTful API built with Node.js, Express, and MongoDB (Mongoose).
Includes features like environment configuration, security middleware, and clean project structure.

## Features
Express.js for routing and middleware
MongoDB connection using Mongoose
Environment variables via dotenv
Secure HTTP headers with Helmet
Response compression for better performance
CORS enabled
User model with validation and timestamps
Ready structure for JWT authentication